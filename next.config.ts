import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Hide the Next.js dev overlay button (the "N" in the bottom-left corner).
  // Dev-only; has no effect on production builds.
  devIndicators: false,
};

export default nextConfig;
